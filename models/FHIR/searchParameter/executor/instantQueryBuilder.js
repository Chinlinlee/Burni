const {
    INSTANT_COMPARATORS,
    splitInstantComparator,
    parseInstantQueryValue,
    buildInstantQuery
} = require("./temporalQuery");

module.exports = {
    INSTANT_COMPARATORS,
    splitInstantComparator,
    parseInstantQueryValue,
    buildInstantQuery
};
